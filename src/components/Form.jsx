import React from "react";

const Form = () => {
  return (
    <section id="section">
      <form>
        <label for="name">Name:</label>
        <input type="text" placeholder="Enter your name" />
      </form>
    </section>
  );
};

export default Form;
